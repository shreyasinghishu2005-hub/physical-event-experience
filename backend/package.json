{
  "name": "physical-event-experience-assistant-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "npm --prefix ../frontend install && npm --prefix ../frontend run build",
    "prestart": "node src/scripts/ensureProductionBuild.js",
    "test": "node --test src/**/*.test.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "next": "15.5.15",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  }
}
